<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        No POS Config form inheritance needed.
        The Many2many relation is fully managed from the User form
        (Settings > Users > Allowed POS tab).
        The reverse field allowed_user_ids on pos.config is still
        available via the ORM / record rules, just not shown as a tab here
        to avoid xpath compatibility issues across Odoo 19 CE editions.
    -->
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- POS restriction is now handled automatically via picking_type_id.warehouse_id -->
    <!-- No custom fields needed on pos.config - avoids POS IndexedDB errors -->
</odoo>

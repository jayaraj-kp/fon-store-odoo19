<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="pos_special_offers.SpecialOfferButton">
        <div>
            <button class="special-offer-btn" t-on-click="onClick">
                🎁 Offers
            </button>
            <!-- Inline popup rendered here to avoid dialog service dependency -->
            <t t-if="state.showPopup">
                <SpecialOfferPopup
                    products="products"
                    categories="categories"
                    close="() => this.closePopup()"
                />
            </t>
        </div>
    </t>

</templates>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="purchase_order_form_date_only" model="ir.ui.view">
        <field name="name">purchase.order.form.date.only</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <!-- Confirmation Date - date only, no time -->
            <xpath expr="//field[@name='date_approve']" position="attributes">
                <attribute name="widget">date</attribute>
            </xpath>

            <!-- Expected Arrival - date only, no time -->
            <xpath expr="//field[@name='date_planned']" position="attributes">
                <attribute name="widget">date</attribute>
            </xpath>

            <!-- Arrival date - date only, no time -->
            <xpath expr="//field[@name='date_calendar_start']" position="attributes">
                <attribute name="widget">date</attribute>
            </xpath>

        </field>
    </record>
</odoo>

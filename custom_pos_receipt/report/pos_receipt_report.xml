<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        This file re-declares the POS receipt report action so it
        uses our inherited template.
        If you only want to *inherit* and patch the existing one,
        this file just needs to exist (data is loaded in order).
        The actual template customisation is in:
            views/report_pos_receipt_template.xml
    -->
</odoo>

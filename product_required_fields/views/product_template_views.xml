<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        NOTE: The required=True attribute is enforced at the Python model level
        in models/product_template.py. This makes both fields required in the
        UI automatically without needing view inheritance.

        The constraints also fire on save via API/imports for full protection.
    -->
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Report menu restrictions are handled entirely by the JavaScript layer
        (access_restrictions.js) which hides menu items by matching their text labels.
        Server-side enforcement is in account_report.py via _get_options() override.
        No ir.rule records needed here — avoids dependency on CE-specific model xmlids.
    -->
</odoo>
